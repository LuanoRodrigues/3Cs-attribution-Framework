import type { LayoutResult } from "../types.ts";

export const relayoutFrom = (_result: LayoutResult, _fromDocPos: number): LayoutResult => {
  // Placeholder hook for incremental relayout.
  return _result;
};
